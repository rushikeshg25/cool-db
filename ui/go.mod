// The Studio contains no Go source. This module exists so that `go build`,
// `go vet`, and `go test ./...` at the repository root stop at ui/ instead of
// descending into ui/node_modules, which vendors Go files of its own.
module github.com/rushikeshg25/coolDb/ui

go 1.23.5
